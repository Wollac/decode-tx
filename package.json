{
  "name": "decode-tx",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://wollac.github.io/decode-tx",
  "dependencies": {
    "@iota/transaction-converter": "^1.0.0-beta.21",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-json-view": "^1.19.1",
    "react-scripts": "3.1.1"
  },
  "scripts": {
    "format": "prettier --single-quote --write src/**/*.js",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "prettier": "^1.18.2"
  }
}
